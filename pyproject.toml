[project]
name = "flood-forecast-system"
version = "0.1.0"
description = "Public hydrology dataset scraped from the CWC Flood Forecast System"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "polars>=1.0",
]
